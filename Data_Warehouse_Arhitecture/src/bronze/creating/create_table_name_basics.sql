create table if not exists bronze_wowcinema.name_basics (
   nconst        varchar(20),
   primaryname   varchar(255),
   birthyear     varchar(4),
   deathyear     varchar(4),
   knowfortitles text
);